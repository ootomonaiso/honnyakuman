name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.0)'
        required: true
        default: 'v1.0.0'

jobs:
  build:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller
      
      - name: Build executable
        run: |
          pyinstaller --noconfirm --onedir --windowed --name "WindowTranslator" --add-data "src;src" main.py
      
      - name: List dist contents
        run: |
          Get-ChildItem -Path dist -Recurse | Select-Object FullName
      
      - name: Create archive
        run: |
          Compress-Archive -Path "dist\WindowTranslator\*" -DestinationPath "WindowTranslator-${{ github.ref_name || inputs.version }}-win64.zip"
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: WindowTranslator-win64
          path: WindowTranslator-*.zip
          retention-days: 5

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'workflow_dispatch'
    
    permissions:
      contents: write
    
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: WindowTranslator-win64
      
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name || inputs.version }}
          name: Window Translator ${{ github.ref_name || inputs.version }}
          body: |
            ## 🌐 Window Translator ${{ github.ref_name || inputs.version }}
            
            Windowsの特定のウィンドウから文字を認識（OCR）して、英語を日本語に翻訳するアプリケーションです。
            
            ### 📥 ダウンロード
            - `WindowTranslator-*-win64.zip` をダウンロードして解凍
            - `WindowTranslator.exe` を実行
            
            ### ✨ 機能
            - ウィンドウキャプチャ & OCR
            - 英語→日本語 自動翻訳
            - オーバーレイ表示
            - GPU加速対応（NVIDIA CUDA）
            
            ### ⚠️ 注意
            - 初回起動時にOCRモデル（約100MB）をダウンロードします
            - GPU加速を使用する場合は別途CUDA版PyTorchが必要です
          files: |
            WindowTranslator-*.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
